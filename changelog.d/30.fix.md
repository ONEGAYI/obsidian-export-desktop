注释转换打断有序列表后编号接续：convert 模式下块级注释合成的 HTML 块会把产物列表物理分段，此前分段后一段重新从起始编号显示（1、2 而非接续的 2、3）；现在注释重开点显式关闭列表并按已发出的列表项数量合成接续起始编号（嵌套列表只分段被打断的最内层，外层保持连续）。strip 模式产物列表本就保持连续、编号按位置递增，行为不变；无序列表不受影响，仅产物在列表与注释块之间多了规范的空行分隔（渲染等价）。
