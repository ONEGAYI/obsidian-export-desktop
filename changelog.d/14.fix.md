修复 `--comments` 在审查中发现的三处正确性缺陷：块级注释开启于容器首段、闭合于同一容器后续段落时，未跨出的祖先容器被空容器消解误删，导致事件流缺层、闭合点后的文本逃逸容器（引用块/列表项场景）；行内格式容器（强调/加粗/删除线）跨越注释边界时半边被吞、流失去平衡，现按差值在注释两侧补回配对半边；注释内容含三个及以上连续连字符时 `--` 折叠会跨插入空格重新拼出 `-->` 提前终结注释并泄漏尾部文本，折叠改为迭代至不动点。另补齐块边界在注释内容中的换行分隔（修复跨容器注释词粘连）、重开容器吞除残余软换行、容器内跨段/跨引用块边界的黄金树场景与回归测试；文档同步（desktop.md / intro.md 双语特性清单、usage-advanced 登记图表预扫描交互与有序列表编号重置）。
