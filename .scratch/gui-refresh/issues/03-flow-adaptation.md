# 适配设置与导出各阶段并完成响应式回归

发布快照：[GitHub #40](https://github.com/ONEGAYI/obsidian-export-desktop/issues/40)。后续以 Issue 正文与状态为准。

Parent spec: #37
Blocked by: #39
状态：blocked。本票交付从设置、确认、导出到链接检查结果的完整适配，包含每条交互路径的验证，不是独立“最后补测试”的空票。

## 实现范围

- 设置页沿用六页导航与 ARIA tabs 键盘交互，使用可伸展页壳和合理阅读宽度；不新增工作台总导航。
- 运行页统一主题，日志利用剩余高度，进度与取消入口保持可见；宽屏允许摘要与日志并排。
- 结果页在检查可见且宽度>=1000px时并排呈现导出结果和链接检查；检查未启用时不保留空列，窄窗堆叠。
- 关于与更新随主题/容器适配，保留代理、更新检查、下载/取消及安装确认。
- 布局及语言改变不重建业务订阅、不重置运行状态，不修改 JSON Lines、计数与互斥规则。

## 验收清单

- [ ] 六类设置都可到达，方向键/Home/End 等既有页签键盘行为通过回归。
- [ ] 运行中缩放窗口、切语言/主题不会丢失或重复事件，取消仍可终止现有任务。
- [ ] 运行日志高度随窗口增长，长日志/错误有滚动而不盖住操作入口。
- [ ] 导出成功、取消、启动失败、部分文件失败均有可读的结果与返回入口。
- [ ] 链接检查运行/完成/取消/解析失败及 IO 错误均可见；结果双栏与窄窗堆叠符合规则。
- [ ] 检查关闭时无空侧栏；开启时仍使用原有来源/产物选择与 start_export 返回落点。
- [ ] 更新检查/下载/取消/安装确认与代理设置保持原有行为；真实烟测不必为验收强行安装更新。
- [ ] 560×480、760×520、1000×520、1440×900、1920×1080 覆盖各阶段的控件可达性，无整体横向溢出。
- [ ] 全部 Vitest、前端 build、先 sync-sidecar 后 desktop-test，以及 `git diff --check` 通过。
- [ ] `python .agents/skills/file-tree/scripts/tree_tool.py check --strict` 通过；相关入口和文件职责更新。
- [ ] 完成按仓库规范与 #37 规格两方面的 code-review，处理有效发现并记录验证证据。
- [ ] 向用户提供可运行结果、截图和测试摘要等待验收；不擅自发布版本。

## 代码入口

`OptionsView.tsx`、`ExportRunView.tsx`、`ExportResultView.tsx`、`LinkCheckPanel.tsx`、`UpdatePanel.tsx`、`App.tsx`、`index.css` 及必要测试。先读 #37 的全部视口矩阵与边界，不在此次改动里顺手修其他业务。

## 交付与关闭

三个 tickets 的实现汇总同一 PR。本票包含最终验收准备，但用户尚未验收前，不把总规格关闭为完成。前置任务、审查或环境有未完成事项时明确记录，不用一张成功截图代替完整证据。
